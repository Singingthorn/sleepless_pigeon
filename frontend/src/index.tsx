import * as React from "react";
import * as ReactDOM from "react-dom";
import { Button } from 'antd';

ReactDOM.render(
  <div> Hello World <Button type="primary">antd</Button> </div>,
  document.getElementById("root")
)